server:
  port: 0

spring:
  application:
    name: USUARIO-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/msvc-usuarios
    username: root
    password: 2357
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  instance:
    prefer-ip-address: true
    instance-id: USUARIO-SERVICE  # ✅ FIX: Ahora está correctamente alineado
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
